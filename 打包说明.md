# Excel数据处理器 - 打包说明

## 快速打包 

### 方法一：使用批处理文件（推荐）
```bash
build.bat
```
这将自动检查依赖并完成打包。

### 方法二：手动安装依赖并打包
```bash
# 1. 安装依赖
pip install pyinstaller openpyxl xlrd

# 2. 执行打包
python build_exe.py
```

### 方法三：一行命令打包（解决openpyxl问题）
```bash
pyinstaller --onefile --windowed --clean --name="Excel数据处理器" --optimize=2 --noconfirm --hidden-import=openpyxl.cell_writer --hidden-import=openpyxl.workbook.workbook --hidden-import=openpyxl.worksheet.worksheet --hidden-import=et_xmlfile --hidden-import=xlrd --hidden-import=tkinter.filedialog --hidden-import=tkinter.messagebox --exclude-module=pytest --exclude-module=unittest --exclude-module=numpy --exclude-module=pandas --exclude-module=matplotlib --exclude-module=requests --exclude-module=PIL --exclude-module=cv2 main.py
```

## 打包选项说明

- `--onefile`: 打包成单个exe文件
- `--windowed`: 无控制台窗口（适合GUI程序）  
- `--clean`: 清理临时文件
- `--optimize=2`: 优化字节码
- `--hidden-import`: 强制包含指定模块（解决openpyxl问题）
- `--exclude-module`: 排除不需要的模块来减小文件体积
- `--noconfirm`: 覆盖输出目录而不询问

## 常见问题

### 如果遇到"No module named 'openpyxl.cell_writer'"错误
这是PyInstaller的已知问题，解决方案：
1. 使用上面的方法一或方法二（已包含修复）
2. 或者在命令中添加 `--hidden-import=openpyxl.cell_writer`

### 如果打包失败
1. 检查Python环境是否正确
2. 确保所有依赖已安装：`pip install -r requirements.txt`
3. 尝试清理构建文件：`python build_exe.py clean`

### 如果exe文件太大
可以在`build_exe.py`中添加更多排除的模块，常见的大型库包括：
- numpy, pandas, scipy
- matplotlib, seaborn  
- requests, urllib3
- PIL, cv2
- tensorflow, torch

### 如果运行时缺少文件
确保模板文件和配置文件被正确添加到打包中。

## 输出位置
打包完成后，exe文件位于 `dist/Excel数据处理器.exe` 